import {PORT, DB_HOST, DB_NAME, DB_PASS, DB_USER, DB_PORT} from './config.js'
import {connect, create} from './db.config.js'

export {PORT, DB_HOST, DB_NAME, DB_PASS, DB_USER, DB_PORT, connect, create}